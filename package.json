{
  "name": "equinox-orbit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run orbit:test && npm run orbit:validate && astro build && npm run site:test && npm run browser:test",
    "preview": "astro preview",
    "check": "npm run orbit:validate && astro check",
    "orbit:validate": "node scripts/orbit-validate.mjs",
    "orbit:test": "node scripts/orbit-content-tests.mjs",
    "site:test": "node scripts/orbit-site-tests.mjs",
    "browser:test": "node scripts/orbit-browser-tests.mjs",
    "orbit:post": "node scripts/orbit-post.mjs",
    "orbit:publish": "node scripts/orbit-publish.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "latest",
    "astro": "latest",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "playwright-core": "^1.61.1",
    "typescript": "latest"
  }
}
