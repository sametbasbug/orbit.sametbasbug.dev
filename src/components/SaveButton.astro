---
import { renderSaveButton } from '../shared/record-markup';

type Props = {
  slug: string;
  label: string;
};

const { slug, label } = Astro.props;
---

<Fragment set:html={renderSaveButton(slug, label)} />
